version: "3.7"

services:
  gitlab-runner:
    image: gitlab/gitlab-runner:latest
    container_name: gitlab-runner
    restart: always
    volumes:
      - ${BASE_VOLUME_DIR}/gitlab-runner/config:/data
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - TZ=Asia/Kuala_Lumpur
